import 'package:Hot_App/ui/home.dart';
import 'package:flutter/material.dart';

main() {
  runApp(hotcon());
}

class hotcon extends StatelessWidget {
  build(BuildContext con) {
    return MyApp();
  }
}
